spring.application.name=resource-service
server.port=8002

spring.datasource.url=${RESOURCE_DB_URL:jdbc:postgresql://localhost:5434/resource_db}
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:1234}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

notification.service.url=${NOTIFICATION_SERVICE_URL:http://localhost:8003}
jwt.secret=${JWT_SECRET:tiendatech_secret_key_2026_microservicios_segura}